export default function Lighting({ enabled }) {


  if (!enabled) return
  else return (
    <>
        {/* <ambientLight intensity={0.6} />
        <directionalLight intensity={0.4} position={[10, 20, 25]} castShadow /> */}
    </>
  )
}
